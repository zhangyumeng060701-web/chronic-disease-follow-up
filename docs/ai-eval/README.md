# AI 随访建议评测

## 目的

验证 AI 随访建议输出满足以下质量要求：

- 必须包含风险分层
- 必须包含 0-1 之间的置信度
- 必须包含判定依据
- 必须包含具体建议
- 医生确认前状态为 `PENDING`

## 运行

```bash
python3 ai-agent/eval/run_suggestion_eval.py
```

当前评测集包含 25 条样例，覆盖高危、中危、稳定、血糖异常、用药依从性异常、无近期数据等场景。
