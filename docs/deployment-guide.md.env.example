# 项目部署指南
本文档用于在新环境下，从零开始一键启动本项目。
## 前置要求
- 安装 Docker Desktop 或 Docker Engine
- 安装 Git
## 快速启动
1. **克隆仓库**
   ```bash
   git clone https://github.com/zhangyumeng060701-web/chronic-disease-follow-up.git
   cd chronic-disease-follow-up

#### 2. 创建 `.env.example`（环境变量模板）
```env
# Spring Boot 应用配置
SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8080

# MySQL 数据库配置（Docker Compose 方式启动时，请使用 `mysql` 作为主机名）
DB_HOST=mysql
DB_PORT=3306
DB_NAME=follow_up
DB_USERNAME=root
DB_PASSWORD=root123

# 华为云 API 密钥（可选，调用对应功能时需要填写）
# HUAWEI_CLOUD_AK=AccessKey
# HUAWEI_CLOUD_SK=SecretKey
