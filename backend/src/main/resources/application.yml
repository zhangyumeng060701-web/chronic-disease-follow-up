server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/follow_up?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: root
    password: 20060701zZ?
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: status
      logic-delete-value: 0
      logic-not-delete-value: 1

knife4j:
  enable: true
  setting:
    language: zh-CN

jwt:
  secret: chronic-disease-follow-up-secret-key-2026-challenge-cup
  expiration: 86400000